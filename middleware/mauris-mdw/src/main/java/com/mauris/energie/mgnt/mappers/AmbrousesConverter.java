package com.mauris.energie.mgnt.mappers;

/**
 * Created by Arnaud on 10.11.2018.
 */
public class AmbrousesConverter {
/*
    public static History toHistory(AmbrosusAnswer ambrosusAnswer){
      History history = new History();
        for (Event value : ambrosusAnswer.getResults())
            convert(value, history);
        return  history;
    }

    private static void convert(Event event, History container){
        Date timeStamp = new Date(event.getContent().getIdData().getTimestamp()*1000);
        for (Mesure masure : event.getContent().getData()){
            container.addMeasure( masure.getObis().toString(),new Measure(timeStamp, masure.getMesure(), masure.getUnit()));
        }
    }
*/
}
